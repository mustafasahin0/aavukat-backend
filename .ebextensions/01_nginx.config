container_commands:
  01_start_nginx:
    command: "systemctl start nginx || true"
    ignoreErrors: true
  02_reload_nginx:
    command: "systemctl reload nginx || systemctl start nginx"
    ignoreErrors: true 